controller_server:
  ros__parameters:
    transform_tolerance: 0.1
    use_sim_time: false
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.0
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["general_goal_checker"]
    controller_plugins: ["FollowPath"]
    odom_topic: /odometry/filtered

    # Progress checker parameters
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.1
      movement_time_allowance: 20.0
    # Goal checker parameters
    general_goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.1
      # Require the final pose heading instead of accepting any orientation.
      yaw_goal_tolerance: 0.15
      stateful: True

    FollowPath:
      # Project-owned controller. Nav2 supplies the path; LOS+PID supplies /cmd_vel.
      plugin: "carrierbot_navigation::LosPdController"
      linear_speed: 0.8
      min_linear_speed: 0.3
      max_angular_speed: 0.5
      kp: 1.0
      ki: 0.05
      kd: 0.7
      integral_limit: 1.0
      wheel_separation: 0.57
      lookahead_min: 0.5
      lookahead_max: 0.8
      lookahead_decay: 0.7
      target_heading_filter_alpha: 0.8
      derivative_filter_alpha: 0.2
      heading_slowdown_angle: 0.5
      initial_rotate_angle: 0.5
      approach_distance: 1.0
      final_align_distance: 0.3
      path_direction: 1.0
      use_final_goal_orientation: false
      transform_tolerance: 0.1


    # FollowPath:
    #   plugin: "dwb_core::DWBLocalPlanner"

    #   # ===== Velocity limits =====
    #   max_vel_x: 0.5
    #   min_vel_x: 0.0
    #   max_vel_theta: 0.5
    #   min_speed_xy: 0.0
    #   max_speed_xy: 0.5
    #   min_speed_theta: 0.0

    #   acc_lim_x: 1.0
    #   acc_lim_theta: 1.0

    #   decel_lim_x: -1.0
    #   decel_lim_theta: -1.0

    #   # ===== Sampling =====
    #   vx_samples: 20
    #   vtheta_samples: 20

    #   sim_time: 1.5   # giống lookahead_time

    #   # ===== Goal tolerance =====
    #   xy_goal_tolerance: 0.25
    #   yaw_goal_tolerance: 0.25
    #   stateful: true

    #   # ===== Critics (quan trọng nhất) =====
    #   critics: [
    #     "RotateToGoal",
    #     "Oscillation",
    #     "BaseObstacle",
    #     "GoalAlign",
    #     "PathAlign",
    #     "PathDist",
    #     "GoalDist"
    #   ]

    #   # ===== Weights =====
    #   PathAlign.scale: 32.0
    #   GoalAlign.scale: 24.0
    #   PathDist.scale: 32.0
    #   GoalDist.scale: 24.0
    #   BaseObstacle.scale: 0.02

    #   RotateToGoal.scale: 32.0
    #   RotateToGoal.slowing_factor: 5.0
    #   RotateToGoal.lookahead_time: 1.0

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_link
      transform_tolerance: 0.5
      use_sim_time: false
      rolling_window: true
      width: 3
      height: 3
      resolution: 0.05
      robot_radius: 0.3
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_topic: /map
        map_subscribe_transient_local: True
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        observation_sources: scan
        scan:
          topic: /scan_filtered
          sensor_frame: laser
          data_type: "LaserScan"
          clearing: True
          marking: True
          obstacle_min_range: 0.5
          obstacle_max_range: 12.0
          raytrace_min_range: 0.5
          raytrace_max_range: 12.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        inflation_radius: 0.6
        cost_scaling_factor: 3.0
      always_send_full_costmap: True
